---
title: "Tipo de cambio, tasas largas y stress macrofinanciero en LatAm"
subtitle: "Modelos diarios para CLP, BRL, MXN, PEN y COP"
description: "Proyecto aplicado que estima desviaciones de tipo de cambio y tasas soberanas a 10 años respecto de fundamentos externos observables en América Latina."
page-layout: full
execute:
  echo: false
  warning: false
  message: false
---

```{r}
#| label: exchange-setup
#| include: false
suppressPackageStartupMessages({
  library(dplyr)
  library(tidyr)
  library(readr)
  library(lubridate)
  library(knitr)
})

has_plotly <- requireNamespace("plotly", quietly = TRUE)
has_htmltools <- requireNamespace("htmltools", quietly = TRUE)

first_existing <- function(paths) {
  x <- paths[file.exists(paths)]
  if (length(x) == 0) return(NA_character_)
  x[[1]]
}

read_exchange_csv <- function(name) {
  p <- first_existing(c(
    file.path("..", "data", "processed", "exchange", name),
    file.path("data", "processed", "exchange", name),
    file.path("..", "..", "data", "processed", "exchange", name)
  ))
  if (is.na(p)) return(NULL)
  readr::read_csv(p, show_col_types = FALSE)
}

fmt_num <- function(x, digits = 3) {
  ifelse(is.na(x), "—", formatC(as.numeric(x), format = "f", digits = digits))
}

fmt_date <- function(x) {
  if (length(x) == 0 || all(is.na(x))) return("—")
  format(as.Date(x), "%d-%m-%Y")
}

country_order <- c("CLP", "BRL", "MXN", "PEN", "COP")
country_label <- c(
  CLP = "Chile · CLP",
  BRL = "Brasil · BRL",
  MXN = "México · MXN",
  PEN = "Perú · PEN",
  COP = "Colombia · COP"
)
country_names <- c(
  CLP = "Chile",
  BRL = "Brasil",
  MXN = "México",
  PEN = "Perú",
  COP = "Colombia"
)
country_cols <- c(
  CLP = "#E67E22",
  BRL = "#3fb1d3",
  MXN = "#006400",
  PEN = "#C4A000",
  COP = "#084e89"
)
market_cols <- c("FX" = "#27384a", "10Y" = "#7b5e42")

residuals_long <- read_exchange_csv("residuals_long.csv")
second_stage_data <- read_exchange_csv("second_stage_data.csv")
fit_summary <- read_exchange_csv("model_fit_summary.csv")
second_summary <- read_exchange_csv("second_stage_summary.csv")
model_coefficients <- read_exchange_csv("model_coefficients.csv")
metadata <- read_exchange_csv("metadata.csv")
latest_snapshot <- read_exchange_csv("latest_snapshot.csv")

data_ready <- !is.null(residuals_long) && !is.null(second_stage_data)
if (data_ready) {
  residuals_long <- residuals_long %>%
    mutate(
      date = as.Date(date),
      country = factor(country, levels = country_order),
      country_label = country_label[as.character(country)],
      market = factor(market, levels = c("FX", "10Y"))
    ) %>%
    arrange(market, country, date)
  second_stage_data <- second_stage_data %>%
    mutate(
      date = as.Date(date),
      country = factor(country, levels = country_order),
      country_label = country_label[as.character(country)]
    ) %>%
    arrange(country, date)
}

latest_model_date <- if (data_ready) max(residuals_long$date, na.rm = TRUE) else NA
sample_start <- if (data_ready) min(residuals_long$date, na.rm = TRUE) else NA

plot_missing <- function(msg = "No se encontraron los datos procesados del modelo.") {
  if (has_htmltools) {
    htmltools::div(
      class = "exchange-missing-card",
      htmltools::strong("Gráfico no disponible"),
      htmltools::p(msg),
      htmltools::p("Ejecuta el modelo desde el repo para generar los CSV en data/processed/exchange/." )
    )
  } else {
    knitr::asis_output(paste0("<p>", msg, "</p>"))
  }
}

plotly_config <- function(p) {
  plotly::config(
    p,
    responsive = TRUE,
    displaylogo = FALSE,
    modeBarButtonsToRemove = c("select2d", "lasso2d", "autoScale2d")
  )
}

plot_global_residuals <- function(market_name, from_date = as.Date("2022-01-01")) {
  if (!data_ready || !has_plotly) return(plot_missing("Falta residuals_long.csv o el paquete plotly."))
  dfp <- residuals_long %>%
    filter(market == market_name, date >= from_date, !is.na(z_score)) %>%
    mutate(country_chr = as.character(country))

  title <- if (market_name == "FX") "Residuos normalizados del tipo de cambio" else "Residuos normalizados de tasas soberanas 10Y"
  ylab <- if (market_name == "FX") "z-score residuo FX" else "z-score residuo 10Y"

  p <- plotly::plot_ly(
    dfp,
    x = ~date,
    y = ~z_score,
    color = ~country_chr,
    colors = country_cols,
    type = "scatter",
    mode = "lines",
    line = list(width = 1.65),
    hovertemplate = paste(
      "País: %{fullData.name}",
      "Fecha: %{x|%d-%m-%Y}",
      "z-score: %{y:.2f}",
      sep = "<br>"
    )
  ) %>%
    plotly::layout(
      height = 520,
      margin = list(l = 64, r = 28, t = 48, b = 56),
      title = list(text = title, x = 0, font = list(size = 18, color = "#27384a")),
      xaxis = list(title = "", showgrid = TRUE, gridcolor = "#e8e4dc"),
      yaxis = list(title = ylab, zeroline = FALSE, showgrid = TRUE, gridcolor = "#e8e4dc"),
      legend = list(orientation = "h", x = 0, y = -0.16),
      paper_bgcolor = "rgba(0,0,0,0)",
      plot_bgcolor = "#ffffff",
      shapes = list(
        list(type = "line", xref = "paper", x0 = 0, x1 = 1, yref = "y", y0 = 0,  y1 = 0,  line = list(color = "#8a94a3", width = 1.1, dash = "dash")),
        list(type = "line", xref = "paper", x0 = 0, x1 = 1, yref = "y", y0 = 2,  y1 = 2,  line = list(color = "#c8c0b3", width = 1, dash = "dot")),
        list(type = "line", xref = "paper", x0 = 0, x1 = 1, yref = "y", y0 = -2, y1 = -2, line = list(color = "#c8c0b3", width = 1, dash = "dot"))
      )
    )

  plotly_config(p)
}

plot_country_stress <- function(cc, from_date = as.Date("2022-01-01")) {
  if (!data_ready || !has_plotly) return(plot_missing("Falta residuals_long.csv o el paquete plotly."))
  dfp <- residuals_long %>%
    filter(country == cc, date >= from_date, !is.na(z_score)) %>%
    mutate(market_chr = as.character(market))

  p <- plotly::plot_ly(
    dfp,
    x = ~date,
    y = ~z_score,
    color = ~market_chr,
    colors = market_cols,
    type = "scatter",
    mode = "lines",
    line = list(width = 1.85),
    hovertemplate = paste(
      "Mercado: %{fullData.name}",
      "Fecha: %{x|%d-%m-%Y}",
      "z-score: %{y:.2f}",
      sep = "<br>"
    )
  ) %>%
    plotly::layout(
      height = 500,
      margin = list(l = 62, r = 24, t = 44, b = 58),
      title = list(text = paste0("Stress conjunto FX y 10Y — ", country_label[[cc]]), x = 0, font = list(size = 17, color = "#27384a")),
      xaxis = list(title = "", showgrid = TRUE, gridcolor = "#e8e4dc"),
      yaxis = list(title = "z-score", zeroline = FALSE, showgrid = TRUE, gridcolor = "#e8e4dc"),
      legend = list(orientation = "h", x = 0, y = -0.16),
      paper_bgcolor = "rgba(0,0,0,0)",
      plot_bgcolor = "#ffffff",
      shapes = list(
        list(type = "line", xref = "paper", x0 = 0, x1 = 1, yref = "y", y0 = 0,  y1 = 0,  line = list(color = "#8a94a3", width = 1.1, dash = "dash")),
        list(type = "line", xref = "paper", x0 = 0, x1 = 1, yref = "y", y0 = 2,  y1 = 2,  line = list(color = "#c8c0b3", width = 1, dash = "dot")),
        list(type = "line", xref = "paper", x0 = 0, x1 = 1, yref = "y", y0 = -2, y1 = -2, line = list(color = "#c8c0b3", width = 1, dash = "dot"))
      )
    )

  plotly_config(p)
}

plot_country_second_stage <- function(cc) {
  if (!data_ready || !has_plotly) return(plot_missing("Falta second_stage_data.csv o el paquete plotly."))
  dfp <- second_stage_data %>%
    filter(country == cc, !is.na(y10_spread), !is.na(z_fx)) %>%
    arrange(y10_spread)
  sm <- if (!is.null(second_summary)) second_summary %>% filter(country == cc) else NULL
  beta <- if (!is.null(sm) && nrow(sm) > 0) fmt_num(sm$beta_y10_spread[1], 3) else "—"
  r2 <- if (!is.null(sm) && nrow(sm) > 0) fmt_num(sm$r2[1], 3) else "—"

  p <- plotly::plot_ly() %>%
    plotly::add_markers(
      data = dfp,
      x = ~y10_spread,
      y = ~z_fx,
      type = "scattergl",
      marker = list(color = "rgba(39,56,74,0.42)", size = 5),
      name = "Observaciones diarias",
      hovertemplate = paste(
        "Fecha: %{customdata|%d-%m-%Y}",
        "Spread 10Y: %{x:.2f} p.p.",
        "Residuo FX: %{y:.2f}",
        sep = "<br>"
      ),
      customdata = ~date
    ) %>%
    plotly::add_lines(
      data = dfp,
      x = ~y10_spread,
      y = ~z_fx_hat,
      line = list(color = "#7b5e42", width = 2.4),
      name = "Ajuste lineal",
      hovertemplate = "Ajuste: %{y:.2f}<extra></extra>"
    ) %>%
    plotly::layout(
      height = 500,
      margin = list(l = 66, r = 24, t = 54, b = 66),
      title = list(
        text = paste0("Segunda etapa — ", country_label[[cc]], "<br><sup>b = ", beta, " · R² = ", r2, "</sup>"),
        x = 0,
        font = list(size = 17, color = "#27384a")
      ),
      xaxis = list(title = "Diferencial 10Y frente a EE.UU. (p.p.)", showgrid = TRUE, gridcolor = "#e8e4dc"),
      yaxis = list(title = "z-score residuo FX", showgrid = TRUE, gridcolor = "#e8e4dc"),
      legend = list(orientation = "h", x = 0, y = -0.18),
      paper_bgcolor = "rgba(0,0,0,0)",
      plot_bgcolor = "#ffffff"
    )

  plotly_config(p)
}

term_labels <- c(
  "trend" = "Tendencia temporal",
  "cpi_rel" = "Precios relativos vs. EE.UU.",
  "l_wti" = "Petróleo WTI",
  "l_pcu" = "Cobre",
  "l_eqnsq" = "Nasdaq",
  "l_eqcny" = "Equity China",
  "l_vix" = "VIX",
  "l_dtw" = "Dólar multilateral",
  "l_cny" = "CNY/USD",
  "y10_us" = "Treasury 10Y EE.UU."
)

coef_table <- function(block_name) {
  if (is.null(model_coefficients)) {
    return(knitr::asis_output("<p>No se encontró <code>model_coefficients.csv</code>.</p>"))
  }
  model_coefficients %>%
    filter(block == block_name, term != "(Intercept)") %>%
    mutate(
      Variable = ifelse(term %in% names(term_labels), term_labels[term], term),
      estimate = as.numeric(estimate),
      p_value = as.numeric(p_value),
      País = country
    ) %>%
    select(País, Variable, Estimación = estimate, `p-value` = p_value) %>%
    mutate(
      Estimación = fmt_num(Estimación, 4),
      `p-value` = fmt_num(`p-value`, 3)
    ) %>%
    arrange(match(País, country_order), Variable) %>%
    knitr::kable(align = "llrr")
}
```

## Qué mide el proyecto

Este proyecto estima una medida comparable de **presión macrofinanciera residual** para cinco economías latinoamericanas. La pregunta central es simple: ¿qué parte de los movimientos del tipo de cambio y de las tasas soberanas largas luce inusual después de controlar por factores externos comunes?

La unidad de análisis no es el movimiento bruto del dólar o de la tasa local, sino el **residuo estandarizado** de modelos de primera etapa. Eso permite comparar países con escalas financieras distintas y separar, al menos de forma reducida, movimientos comunes globales de episodios que requieren una lectura local más cuidadosa.

::: {.mu-kpi-grid}
::: {.mu-kpi-card}
::: {.mu-kpi-label}
Cobertura
:::
::: {.mu-kpi-value}
5 países
:::
Chile, Brasil, México, Perú y Colombia.
:::

::: {.mu-kpi-card}
::: {.mu-kpi-label}
Mercados
:::
::: {.mu-kpi-value}
FX + 10Y
:::
Tipo de cambio bilateral y tasa soberana larga.
:::

::: {.mu-kpi-card}
::: {.mu-kpi-label}
Frecuencia
:::
::: {.mu-kpi-value}
Diaria
:::
Series financieras armonizadas desde 2010.
:::

::: {.mu-kpi-card}
::: {.mu-kpi-label}
Última observación
:::
::: {.mu-kpi-value}
`r fmt_date(latest_model_date)`
:::
Según los datos procesados disponibles.
:::
:::

## Estructura del modelo

::: {.mu-equation-card}
### Primera etapa: tipo de cambio

$$
\log S_{i,t}
= \alpha_i + \beta_i t
+ \rho_i \log\left(\frac{P_{i,t}}{P_{US,t}}\right)
+ \gamma_{1i}\log(WTI_t)
+ \gamma_{2i}\log(Cu_t)
+ \gamma_{3i}\log(Nasdaq_t)
+ \gamma_{4i}\log(ChinaEq_t)
+ \gamma_{5i}\log(VIX_t)
+ \gamma_{6i}\log(DXY_t)
+ \gamma_{7i}\log(CNY_t)
+ u^{FX}_{i,t}
$$

$S_{i,t}$ es el tipo de cambio bilateral frente al dólar, medido como moneda local por USD. El término $P_{i,t}/P_{US,t}$ captura precios relativos frente a EE.UU. y el resto de controles resume factores externos comunes: commodities, equity global/China, volatilidad, dólar multilateral y CNY/USD.
:::

::: {.mu-equation-card}
### Primera etapa: tasa soberana 10Y

$$
y^{10}_{i,t}
= \delta_i + \phi_i y^{10}_{US,t}
+ \theta_{1i}\log(VIX_t)
+ \theta_{2i}\log(Nasdaq_t)
+ \theta_{3i}\log(DXY_t)
+ \theta_{4i}\log(CNY_t)
+ \eta_i t
+ u^{10Y}_{i,t}
$$

La tasa local a 10 años se compara con la Treasury 10Y y con condiciones financieras globales. La intención no es explicar toda la curva local, sino construir una métrica comparable de desviación respecto de factores externos observables.
:::

::: {.mu-equation-card}
### Normalización y segunda etapa

$$
z^{FX}_{i,t}=\frac{u^{FX}_{i,t}-\bar u^{FX}_{i}}{\sigma(u^{FX}_{i})},
\qquad
z^{10Y}_{i,t}=\frac{u^{10Y}_{i,t}-\bar u^{10Y}_{i}}{\sigma(u^{10Y}_{i})}
$$

$$
z^{FX}_{i,t}
= a_i + b_i\left(y^{10}_{i,t}-y^{10}_{US,t}\right)+e_{i,t}
$$

La segunda etapa es descriptiva: resume si la presión cambiaria residual tiende a coincidir con un mayor spread soberano 10Y frente a EE.UU. No debe interpretarse como causalidad estructural.
:::

## Estimaciones principales

### Ajuste de primera etapa

```{r}
#| label: exchange-fit-summary
if (!is.null(fit_summary) && all(c("country", "block", "r2", "rmse") %in% names(fit_summary))) {
  fit_tab <- fit_summary %>%
    mutate(
      country = factor(country, levels = country_order),
      block = factor(block, levels = c("FX", "10Y"))
    ) %>%
    arrange(country, block) %>%
    select(País = country, Modelo = block, Observaciones = n_obs, `R²` = r2, `R² ajustado` = adj_r2, RMSE = rmse) %>%
    mutate(
      `R²` = fmt_num(`R²`, 3),
      `R² ajustado` = fmt_num(`R² ajustado`, 3),
      RMSE = fmt_num(RMSE, 3)
    )
  knitr::kable(fit_tab, align = "llrrrr")
} else {
  knitr::asis_output("<p>No se encontró la tabla de ajuste. Ejecuta el modelo para regenerar <code>model_fit_summary.csv</code>.</p>")
}
```

### Coeficientes de primera etapa

Los coeficientes se presentan con nombres económicos, no con los nombres internos del script. La lectura debe ser prudente: son modelos reducidos para extraer residuos comparables, no una identificación estructural de cada canal.

::: {.panel-tabset .exchange-estimation-tabs}

#### Tipo de cambio

```{r}
#| label: exchange-coefs-fx
coef_table("FX")
```

#### Tasa soberana 10Y

```{r}
#| label: exchange-coefs-y10
coef_table("10Y")
```

:::

### Segunda etapa: residuo FX y spread soberano 10Y

```{r}
#| label: exchange-second-stage-summary
if (!is.null(second_summary) && all(c("country", "beta_y10_spread", "p_value", "r2", "n_obs") %in% names(second_summary))) {
  second_tab <- second_summary %>%
    mutate(country = factor(country, levels = country_order)) %>%
    arrange(country) %>%
    transmute(
      País = country,
      `Coeficiente bᵢ` = fmt_num(beta_y10_spread, 3),
      `p-value` = fmt_num(p_value, 3),
      `R²` = fmt_num(r2, 3),
      Observaciones = n_obs,
      Lectura = lectura
    )
  knitr::kable(second_tab, align = "lrrrll")
} else {
  knitr::asis_output("<p>No se encontró la tabla de segunda etapa. Ejecuta el modelo para regenerar <code>second_stage_summary.csv</code>.</p>")
}
```

## Dashboard regional interactivo

Las líneas punteadas en ±2 desviaciones estándar funcionan como referencia visual de episodios extremos. No son una regla mecánica de crisis; sirven para priorizar dónde mirar.

::: {.exchange-chart-card}
```{r}
#| label: exchange-global-fx
plot_global_residuals("FX")
```
:::

::: {.exchange-chart-card}
```{r}
#| label: exchange-global-y10
plot_global_residuals("10Y")
```
:::

## Selector por país

Cada pestaña mantiene la misma plantilla: primero el stress conjunto de FX y 10Y; después la segunda etapa entre residuo cambiario y spread soberano 10Y frente a EE.UU. Los gráficos se construyen directamente desde los CSV del modelo al renderizar la página.

::: {.panel-tabset .exchange-country-tabs}

### Chile · CLP

::: {.exchange-chart-card}
```{r}
#| label: exchange-country-clp-stress
plot_country_stress("CLP")
```
:::

::: {.exchange-chart-card}
```{r}
#| label: exchange-country-clp-second
plot_country_second_stage("CLP")
```
:::

### Brasil · BRL

::: {.exchange-chart-card}
```{r}
#| label: exchange-country-brl-stress
plot_country_stress("BRL")
```
:::

::: {.exchange-chart-card}
```{r}
#| label: exchange-country-brl-second
plot_country_second_stage("BRL")
```
:::

### México · MXN

::: {.exchange-chart-card}
```{r}
#| label: exchange-country-mxn-stress
plot_country_stress("MXN")
```
:::

::: {.exchange-chart-card}
```{r}
#| label: exchange-country-mxn-second
plot_country_second_stage("MXN")
```
:::

### Perú · PEN

::: {.exchange-chart-card}
```{r}
#| label: exchange-country-pen-stress
plot_country_stress("PEN")
```
:::

::: {.exchange-chart-card}
```{r}
#| label: exchange-country-pen-second
plot_country_second_stage("PEN")
```
:::

### Colombia · COP

::: {.exchange-chart-card}
```{r}
#| label: exchange-country-cop-stress
plot_country_stress("COP")
```
:::

::: {.exchange-chart-card}
```{r}
#| label: exchange-country-cop-second
plot_country_second_stage("COP")
```
:::

:::

```{=html}
<script>
document.addEventListener('shown.bs.tab', function () {
  window.dispatchEvent(new Event('resize'));
  if (window.Plotly) {
    document.querySelectorAll('.js-plotly-plot').forEach(function(el) {
      try { window.Plotly.Plots.resize(el); } catch(e) {}
    });
  }
});
</script>
```

## Lectura económica

El valor del ejercicio está en ordenar la discusión macrofinanciera regional. Un residuo extremo no prueba por sí solo un shock local; indica que, bajo una especificación común, el movimiento observado no queda bien explicado por los factores externos incluidos. Ese punto puede abrir una revisión de liquidez local, riesgo fiscal, eventos políticos, expectativas monetarias, controles de capital, regulación o shocks idiosincráticos.

::: {.mu-grid .mu-grid-areas}

::: {.mu-area}
### Qué permite
Comparar países con una misma especificación, detectar episodios residuales extremos y separar visualmente presión cambiaria de presión en tasas largas.
:::

::: {.mu-area}
### Qué no permite
No identifica causalidad estructural ni separa perfectamente riesgo político, fiscal, liquidez, intervención cambiaria o errores de medición.
:::

::: {.mu-area}
### Próximo salto natural
Agregar validación fuera de muestra, ventanas rolling, EMBI/CDS y un índice sintético regional con umbrales explícitos por régimen.
:::

:::
