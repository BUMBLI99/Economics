---
title: "Tipo de cambio, tasas largas y stress macrofinanciero en LatAm"
subtitle: "Modelos diarios para CLP, BRL, MXN, PEN y COP"
description: "Proyecto aplicado que estima desviaciones de tipo de cambio y tasas soberanas a 10 años respecto de fundamentos externos observables en América Latina."
image: "../assets/img/exchange/fx_residuals_zscores_from2018.jpg"
page-layout: full
execute:
  echo: false
  warning: false
  message: false
---

```{r exchange-helpers, include=FALSE}
first_existing <- function(paths) {
  paths[file.exists(paths)][1]
}

fmt_num <- function(x, digits = 3) {
  ifelse(is.na(x), "—", formatC(as.numeric(x), format = "f", digits = digits))
}

read_exchange_csv <- function(name) {
  p <- first_existing(c(
    file.path("..", "data", "processed", "exchange", name),
    file.path("data", "processed", "exchange", name),
    file.path("..", "..", "data", "processed", "exchange", name)
  ))
  if (is.na(p)) return(NULL)
  read.csv(p, stringsAsFactors = FALSE, check.names = FALSE)
}

asset_img <- function(file) file.path("..", "assets", "img", "exchange", file)

country_meta <- data.frame(
  country = c("CLP", "BRL", "MXN", "PEN", "COP"),
  label   = c("Chile · CLP", "Brasil · BRL", "México · MXN", "Perú · PEN", "Colombia · COP"),
  lectura = c(
    "La presión cambiaria residual y el spread 10Y muestran una asociación positiva. La lectura es útil como monitoreo macrofinanciero, no como identificación causal.",
    "La relación FX residual–spread 10Y es débil en esta especificación. Brasil probablemente requiere controles locales adicionales, especialmente riesgo fiscal, política doméstica y primas de riesgo idiosincráticas.",
    "La asociación es positiva y relativamente clara. Es consistente con episodios donde depreciación residual y aumento del spread largo se mueven conjuntamente.",
    "Es el caso con vínculo más marcado en la segunda etapa. La interpretación debe considerar la menor volatilidad cambiaria relativa y la gestión activa del mercado FX.",
    "La asociación es positiva pero económicamente pequeña. La lectura por país debería complementarse con petróleo, fiscal, liquidez local y primas de riesgo soberano."
  ),
  stringsAsFactors = FALSE
)
```

## Qué mide el proyecto

Este proyecto compara movimientos de **tipo de cambio** y **tasas soberanas a 10 años** en cinco economías latinoamericanas. La idea no es mirar depreciaciones o aumentos de tasas en bruto, sino separar una parte explicada por fundamentos externos observables y una parte residual estandarizada.

El residuo no debe leerse mecánicamente como “shock doméstico puro”. Es una métrica de monitoreo: cuando el residuo se vuelve persistente o extremo, el caso merece una revisión más detallada de factores locales, liquidez, riesgo político, sorpresas fiscales o shocks regulatorios.

::: {.mu-kpi-grid}
::: {.mu-kpi-card}
::: {.mu-kpi-label}
Cobertura
:::
::: {.mu-kpi-value}
5 países
:::
Chile, Brasil, México, Perú y Colombia.
:::

::: {.mu-kpi-card}
::: {.mu-kpi-label}
Mercados
:::
::: {.mu-kpi-value}
FX + 10Y
:::
Tipo de cambio bilateral y tasa soberana larga.
:::

::: {.mu-kpi-card}
::: {.mu-kpi-label}
Frecuencia
:::
::: {.mu-kpi-value}
Diaria
:::
Series financieras armonizadas desde 2010.
:::

::: {.mu-kpi-card}
::: {.mu-kpi-label}
Salida central
:::
::: {.mu-kpi-value}
z-score
:::
Residuo normalizado por país y mercado.
:::
:::

## Especificación del modelo

La estimación se organiza en dos etapas. La primera etapa obtiene residuos de tipo de cambio y de tasa larga para cada país. La segunda etapa resume la asociación entre presión cambiaria residual y spread soberano 10Y frente a EE.UU.

::: {.mu-equation-card}
### Primera etapa: tipo de cambio

$$
\log S_{i,t}
= \alpha_i + \beta_i t
+ \rho_i \log\left(\frac{CPI_{i,t}}{CPI_{US,t}}\right)
+ \Gamma_i' X_t
+ u^{FX}_{i,t}
$$

Donde $S_{i,t}$ es el tipo de cambio bilateral frente al dólar, expresado como moneda local por USD. El término de precios relativos aproxima presiones de inflación relativa contra EE.UU. El vector $X_t$ agrupa fundamentos externos comunes: petróleo WTI, cobre, Nasdaq, equity China, VIX, dólar multilateral y CNY/USD.

El indicador publicado es el residuo estandarizado:

$$
z^{FX}_{i,t}=\frac{u^{FX}_{i,t}-\bar u^{FX}_{i}}{\sigma(u^{FX}_{i})}
$$
:::

::: {.mu-equation-card}
### Primera etapa: tasa soberana 10Y

$$
y^{10}_{i,t}
= \delta_i + \phi_i y^{10}_{US,t}
+ \Theta_i' G_t
+ \eta_i t
+ u^{10Y}_{i,t}
$$

Donde $y^{10}_{i,t}$ es la tasa soberana local a 10 años y $y^{10}_{US,t}$ es la tasa Treasury 10Y. El vector $G_t$ incluye factores globales financieros: VIX, Nasdaq, dólar multilateral y CNY/USD. La salida comparable entre países es:

$$
z^{10Y}_{i,t}=\frac{u^{10Y}_{i,t}-\bar u^{10Y}_{i}}{\sigma(u^{10Y}_{i})}
$$
:::

::: {.mu-equation-card}
### Segunda etapa: presión cambiaria y spread largo

$$
z^{FX}_{i,t}
= a_i + b_i\left(y^{10}_{i,t}-y^{10}_{US,t}\right)+e_{i,t}
$$

Esta ecuación es descriptiva. Resume si los episodios de presión cambiaria residual tienden a coincidir con un mayor spread soberano largo frente a EE.UU. No identifica causalidad estructural.
:::

## Dashboard regional

Los gráficos globales muestran la sincronización regional de los residuos. Las líneas en torno a ±2 desvíos estándar funcionan como referencia visual de episodios extremos; no son una regla mecánica de crisis.

### Tipo de cambio

::: {.exchange-wide-chart}
![Residuos normalizados del tipo de cambio](../assets/img/exchange/fx_residuals_zscores_from2018.jpg)
:::

### Tasas soberanas 10Y

::: {.exchange-wide-chart}
![Residuos normalizados de tasas soberanas 10Y](../assets/img/exchange/y10_residuals_zscores_from2018.jpg)
:::

## Estimaciones principales

### Ajuste de primera etapa

```{r exchange-fit-table}
fit <- read_exchange_csv("model_fit_summary.csv")
if (!is.null(fit) && all(c("country", "block", "r2", "rmse") %in% names(fit)) && requireNamespace("knitr", quietly = TRUE)) {
  countries <- c("CLP", "BRL", "MXN", "PEN", "COP")
  rows <- lapply(countries, function(cc) {
    fx <- fit[fit$country == cc & fit$block == "FX", ]
    y10 <- fit[fit$country == cc & fit$block == "10Y", ]
    data.frame(
      País = cc,
      `FX R²` = fmt_num(fx$r2[1], 3),
      `FX RMSE` = fmt_num(fx$rmse[1], 2),
      `10Y R²` = fmt_num(y10$r2[1], 3),
      `10Y RMSE` = fmt_num(y10$rmse[1], 2),
      check.names = FALSE
    )
  })
  knitr::kable(do.call(rbind, rows), align = "lrrrr")
} else {
  knitr::asis_output("<p>No se encontró la tabla de ajuste. Ejecuta <code>.\\render_exchange_site.bat</code> para regenerar outputs.</p>")
}
```

El ajuste del bloque cambiario suele ser mayor que el de tasas largas. Esto es razonable: las tasas soberanas incorporan prima de riesgo local, expectativas fiscales, liquidez y shocks de régimen monetario que una especificación global lineal no captura completamente.

### Segunda etapa

```{r exchange-second-table}
second <- read_exchange_csv("second_stage_summary.csv")
if (!is.null(second) && all(c("country", "beta_y10_spread", "r2", "lectura") %in% names(second)) && requireNamespace("knitr", quietly = TRUE)) {
  tab <- data.frame(
    País = second$country,
    `Coeficiente bᵢ` = fmt_num(second$beta_y10_spread, 3),
    `R²` = fmt_num(second$r2, 3),
    Lectura = second$lectura,
    check.names = FALSE
  )
  knitr::kable(tab, align = "lrrl")
} else {
  knitr::asis_output("<p>No se encontró la tabla de segunda etapa.</p>")
}
```

El coeficiente $b_i$ resume cuánto se asocia el residuo cambiario normalizado con el spread soberano 10Y. Un coeficiente positivo indica que, en esa muestra, mayores spreads largos tienden a coincidir con mayor presión cambiaria residual.

## Selector por país

Usa el selector para revisar cada país con la misma plantilla: primero el stress conjunto de FX y 10Y, luego la segunda etapa entre residuo cambiario y spread 10Y frente a EE.UU.

```{r exchange-country-selector, results='asis'}
second <- read_exchange_csv("second_stage_summary.csv")
if (is.null(second)) second <- data.frame(country = country_meta$country, beta_y10_spread = NA_real_, r2 = NA_real_)
meta <- merge(country_meta, second[, intersect(names(second), c("country", "beta_y10_spread", "r2"))], by = "country", all.x = TRUE, sort = FALSE)
meta <- meta[match(country_meta$country, meta$country), ]

options_html <- paste0(
  sprintf('<option value="%s"%s>%s</option>',
          meta$country,
          ifelse(meta$country == "CLP", ' selected', ''),
          meta$label),
  collapse = "\n"
)

panel_html <- paste(vapply(seq_len(nrow(meta)), function(i) {
  cc <- meta$country[i]
  beta <- fmt_num(meta$beta_y10_spread[i], 3)
  r2 <- fmt_num(meta$r2[i], 3)
  paste0(
    '<section class="exchange-country-panel', if (cc == "CLP") ' is-active' else '', '" data-country="', cc, '">',
    '<div class="exchange-country-header"><span>', meta$label[i], '</span><strong>Segunda etapa: bᵢ = ', beta, ' · R² = ', r2, '</strong></div>',
    '<p>', meta$lectura[i], '</p>',
    '<div class="exchange-country-figures">',
      '<figure class="exchange-country-figure exchange-panorama"><img src="../assets/img/exchange/stress_fx_y10_', cc, '.jpg" alt="Stress FX y 10Y ', cc, '"><figcaption>Stress conjunto: residuo FX y residuo 10Y normalizados.</figcaption></figure>',
      '<figure class="exchange-country-figure exchange-panorama"><img src="../assets/img/exchange/second_stage_fx_y10_', cc, '.jpg" alt="Segunda etapa FX y spread 10Y ', cc, '"><figcaption>Segunda etapa: residuo FX normalizado frente al diferencial 10Y con EE.UU.</figcaption></figure>',
    '</div>',
    '</section>'
  )
}, character(1)), collapse = "\n")

cat(paste0(
  '<div class="exchange-selector-card" id="exchange-selector-card">',
  '<div class="exchange-selector-row"><label for="exchange-country-select">País</label><select id="exchange-country-select" class="exchange-select">',
  options_html,
  '</select></div>',
  panel_html,
  '</div>',
  '<script>',
  '(function(){',
  'var card=document.getElementById("exchange-selector-card");',
  'var selector=document.getElementById("exchange-country-select");',
  'if(!card||!selector){return;}',
  'var panels=card.querySelectorAll(".exchange-country-panel");',
  'card.classList.add("exchange-js");',
  'function showCountry(country){for(var i=0;i<panels.length;i++){panels[i].classList.toggle("is-active",panels[i].getAttribute("data-country")===country);}}',
  'selector.addEventListener("change",function(e){showCountry(e.target.value);});',
  'showCountry(selector.value);',
  '})();',
  '</script>'
))
```

## Interpretación económica

La contribución del proyecto es disciplinar la lectura de stress macrofinanciero regional. En vez de mirar movimientos financieros aislados, el ejercicio pregunta si esos movimientos son inusuales después de controlar por un conjunto común de fundamentos externos.

::: {.mu-grid .mu-grid-areas}
::: {.mu-area}
### Qué sí permite
Comparar países bajo una misma especificación, detectar episodios residuales extremos y ordenar la revisión de eventos locales.
:::

::: {.mu-area}
### Qué no permite
No identifica causalidad estructural ni separa perfectamente shocks domésticos, riesgo político, liquidez, fiscal o errores de medición.
:::

::: {.mu-area}
### Próximo salto
Agregar validación fuera de muestra, ventanas rolling, EMBI/CDS y un índice sintético regional de stress con umbrales explícitos.
:::
:::

[Descargar reporte PDF](../assets/files/exchange_report.pdf){.mu-button .mu-button-secondary}
