---
title: "Curva de rendimiento chilena interactiva — en desarrollo"
subtitle: "Prototipo visual de tasas nominales y reales por plazo"
description: "Prototipo de visualización para curva de rendimiento chilena, pendiente de curva y señales macrofinancieras."
image: "../assets/img/og/portfolio-og.png"
---

::: {.callout-warning}
## Línea en desarrollo

Esta página se mantiene deliberadamente como prototipo. No debe presentarse como evidencia empírica hasta reemplazar la simulación por datos reales de curvas nominales, reales/UF y compensación inflacionaria.
:::

## Motivación

Este proyecto busca visualizar la evolución de la estructura temporal de tasas en Chile y extraer señales macrofinancieras relevantes.

La curva de rendimiento permite observar cambios en condiciones financieras, expectativas de tasas, pendiente de curva y episodios de aplanamiento o empinamiento.

::: {.mu-note}
**Estado:** prototipo visual. La figura actual usa datos simulados para validar interacción, diseño y estructura de navegación. Antes de presentarlo como evidencia aplicada, debe conectarse a tasas nominales y reales chilenas publicadas por fuentes oficiales o de mercado.
:::

## Prototipo visual: curva por fecha

```{r}
#| echo: false
#| message: false
#| warning: false

library(tidyverse)
library(plotly)

set.seed(7)
fechas <- seq.Date(as.Date("2021-01-01"), as.Date("2026-05-01"), by = "quarter")
plazos <- c(1, 2, 5, 10)

curvas <- expand_grid(fecha = fechas, plazo = plazos) |> 
  mutate(
    ciclo = as.numeric(scale(as.numeric(fecha))),
    tasa = 3.5 + 0.25 * plazo + 1.2 * sin(as.numeric(fecha) / 260) - 0.06 * plazo * sin(as.numeric(fecha) / 400) + rnorm(n(), 0, .08),
    fecha_lab = format(fecha, "%Y-%m")
  )

plot_ly(
  curvas,
  x = ~plazo,
  y = ~tasa,
  split = ~fecha_lab,
  frame = ~fecha_lab,
  type = "scatter",
  mode = "lines+markers",
  hovertemplate = "Plazo: %{x} años<br>Tasa del prototipo: %{y:.2f}%<extra></extra>"
) |> 
  layout(
    title = "Curva de rendimiento chilena: prototipo interactivo",
    xaxis = list(title = "Plazo, años"),
    yaxis = list(title = "Tasa, %")
  )
```

## Próximas mejoras

- Incorporar curva nominal y curva real/UF.
- Calcular pendiente 10y-2y.
- Mostrar breakeven inflation aproximado, con advertencia sobre primas de riesgo y liquidez.
- Marcar fechas de episodios macrofinancieros relevantes.
