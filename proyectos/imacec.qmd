---
title: "Nowcasting de actividad económica en Chile"
subtitle: "Modelo mensual en R para seguimiento coyuntural del IMACEC total y no minero"
execute:
  echo: false
  message: false
  warning: false
format:
  html:
    toc: true
    page-layout: full
description: "Modelo mensual de nowcasting del IMACEC total y no minero con indicadores sectoriales, variables calendario y evaluación de ajuste."
image: "../assets/img/og/portfolio-og.png"
---

```{r setup}
#| include: false
#| message: false
#| warning: false

library(dplyr)
library(readr)
library(ggplot2)
library(plotly)
library(scales)
library(lubridate)
library(knitr)

# -------------------------------------------------------------------
# Detectar automáticamente la raíz del repositorio Economics
# -------------------------------------------------------------------

find_project_root <- function(path = getwd()) {
  path <- normalizePath(path, winslash = "/", mustWork = TRUE)
  
  for (i in seq_len(10)) {
    if (file.exists(file.path(path, "_quarto.yml"))) {
      return(path)
    }
    
    parent <- dirname(path)
    
    if (identical(parent, path)) {
      break
    }
    
    path <- parent
  }
  
  stop("No pude encontrar la raíz del proyecto. Revisa que exista _quarto.yml.")
}

project_root <- find_project_root()

# Hace que los chunks siguientes trabajen desde la raíz del proyecto
knitr::opts_knit$set(root.dir = project_root)

# Helper para construir rutas robustas
path_project <- function(...) {
  file.path(project_root, ...)
}

# -------------------------------------------------------------------
# Elegir archivos reales si existen; si no, usar archivos demo
# -------------------------------------------------------------------

choose_file <- function(real_path, demo_path) {
  real_full <- path_project(real_path)
  demo_full <- path_project(demo_path)
  
  if (file.exists(real_full)) {
    list(path = real_full, status = "real")
  } else if (file.exists(demo_full)) {
    list(path = demo_full, status = "demo")
  } else {
    stop(
      "No existe ni el archivo real ni el archivo demo:\n",
      "- Real: ", real_full, "\n",
      "- Demo: ", demo_full
    )
  }
}

history_file <- choose_file(
  "data/processed/imacec_nowcast_history.csv",
  "data/processed/imacec_nowcast_history_demo.csv"
)

summary_file <- choose_file(
  "data/processed/imacec_nowcast_summary.csv",
  "data/processed/imacec_nowcast_summary_demo.csv"
)

metrics_file <- choose_file(
  "data/processed/imacec_model_metrics.csv",
  "data/processed/imacec_model_metrics_demo.csv"
)

oos_file <- choose_file(
  "data/processed/imacec_oos_metrics.csv",
  "data/processed/imacec_oos_metrics_demo.csv"
)

projection_file <- choose_file(
  "data/processed/imacec_projection.csv",
  "data/processed/imacec_projection_demo.csv"
)

data_status <- history_file$status

# -------------------------------------------------------------------
# Leer datos
# -------------------------------------------------------------------

hist <- readr::read_csv(history_file$path, show_col_types = FALSE) |>
  mutate(Periodo = as.Date(Periodo))

resumen <- readr::read_csv(summary_file$path, show_col_types = FALSE) |>
  mutate(Periodo = as.Date(Periodo))

metricas <- readr::read_csv(metrics_file$path, show_col_types = FALSE)

oos_metricas <- readr::read_csv(oos_file$path, show_col_types = FALSE) |>
  mutate(
    inicio = as.Date(inicio),
    fin = as.Date(fin)
  )

proyeccion <- readr::read_csv(projection_file$path, show_col_types = FALSE) |>
  mutate(Periodo = as.Date(Periodo))
```

::: {.callout-note}
## Estado del proyecto

**Proyecto aplicado en desarrollo continuo.** La metodología base está implementada para actualizarse mensualmente con nueva información. La página presenta la pregunta económica, la estructura del modelo, los resultados principales y una evaluación preliminar de desempeño.
:::

```{r data-status, results='asis'}
if (identical(data_status, "demo")) {
  cat(
    "::: {.callout-warning}\n",
    "## Resultados de ejemplo\n\n",
    "Todavía no se han generado los archivos reales `data/processed/imacec_nowcast_*.csv`. ",
    "Por ahora esta página muestra resultados de ejemplo hasta que se actualice la base procesada del proyecto.\n",
    ":::"
  )
}
```

## Pregunta económica

El IMACEC es una de las señales principales para evaluar la coyuntura económica chilena. Este proyecto busca responder una pregunta práctica:

> ¿Es posible anticipar la evolución reciente del IMACEC total y no minero usando información mensual disponible antes de la publicación oficial?

El objetivo no es producir una proyección oficial, sino construir una herramienta reproducible de seguimiento coyuntural que combine datos macroeconómicos, variables calendario, indicadores sectoriales y modelos econométricos simples.

## Resultado principal

```{r summary-table}
resumen |>
  mutate(Periodo = format(Periodo, "%Y-%m")) |>
  knitr::kable(
    digits = 2,
    col.names = c("Período", "IMACEC total", "IMACEC no minero", "Tipo"),
    caption = "Últimas observaciones y nowcast mensual"
  )
```

## IMACEC total: observado versus ajuste/nowcast

```{r plot-total}
hist_plot <- hist |>
  filter(Periodo >= max(Periodo, na.rm = TRUE) %m-% months(95))

g_total <- ggplot(hist_plot, aes(x = Periodo)) +
  geom_hline(yintercept = 0, linewidth = 0.25, color = "grey70") +
  geom_line(aes(y = imacec, color = "Observado"), linewidth = 0.75, na.rm = TRUE) +
  geom_line(aes(y = imacec_fit, color = "Ajuste / nowcast"), linewidth = 0.75, linetype = "dashed", na.rm = TRUE) +
  geom_point(
    data = filter(hist_plot, tipo == "Nowcast"),
    aes(y = imacec_fit, color = "Nowcast"),
    size = 2.5,
    na.rm = TRUE
  ) +
  scale_color_manual(values = c("Observado" = "#1f4e79", "Ajuste / nowcast" = "#b03a2e", "Nowcast" = "#7f1d1d")) +
  labs(
    x = NULL,
    y = "Var. 12m (%)",
    color = NULL,
    title = "IMACEC total: observado versus ajuste/nowcast"
  ) +
theme_minimal(base_size = 12) +
theme(
  legend.position = "bottom",
  legend.direction = "horizontal",
  legend.title = element_blank(),
  plot.title = element_text(face = "bold", margin = margin(b = 12)),
  panel.grid.minor = element_blank()
)

plotly::ggplotly(g_total) |>
  plotly::layout(
    legend = list(
      orientation = "h",
      x = 0.5,
      xanchor = "center",
      y = -0.22,
      yanchor = "top"
    ),
    margin = list(t = 70, b = 90)
  )
```

## IMACEC no minero: observado versus ajuste/nowcast

```{r plot-nm}
g_nm <- ggplot(hist_plot, aes(x = Periodo)) +
  geom_hline(yintercept = 0, linewidth = 0.25, color = "grey70") +
  geom_line(aes(y = imacec_nm, color = "Observado"), linewidth = 0.75, na.rm = TRUE) +
  geom_line(aes(y = imacec_nm_fit, color = "Ajuste / nowcast"), linewidth = 0.75, linetype = "dashed", na.rm = TRUE) +
  geom_point(
    data = filter(hist_plot, tipo == "Nowcast"),
    aes(y = imacec_nm_fit, color = "Nowcast"),
    size = 2.5,
    na.rm = TRUE
  ) +
  scale_color_manual(values = c("Observado" = "#1f4e79", "Ajuste / nowcast" = "#b03a2e", "Nowcast" = "#7f1d1d")) +
  labs(
    x = NULL,
    y = "Var. 12m (%)",
    color = NULL,
    title = "IMACEC no minero: observado versus ajuste/nowcast"
  ) +
theme_minimal(base_size = 12) +
theme(
  legend.position = "bottom",
  legend.direction = "horizontal",
  legend.title = element_blank(),
  plot.title = element_text(face = "bold", margin = margin(b = 12)),
  panel.grid.minor = element_blank()
)

plotly::ggplotly(g_nm) |>
  plotly::layout(
    legend = list(
      orientation = "h",
      x = 0.5,
      xanchor = "center",
      y = -0.22,
      yanchor = "top"
    ),
    margin = list(t = 70, b = 90)
  )
```

## Datos utilizados

El proyecto integra información mensual de actividad, comercio, crédito, precios, mercado laboral e indicadores sectoriales. La versión actual considera:

- IMACEC total e IMACEC no minero.
- Ventas minoristas.
- Flujo y número de operaciones de crédito.
- UF e inflación implícita asociada.
- Desempleo.
- Cobre y petróleo.
- Días hábiles y variables calendario.
- Indicadores sectoriales INE: minería, manufactura, comercio y electricidad.

## Enfoque metodológico

La versión actual implementa modelos lineales de nowcasting con rezagos de actividad, indicadores contemporáneos, dummies mensuales, efectos calendario y controles puntuales para episodios atípicos. También incluye una especificación alternativa con indicadores sectoriales INE.

El flujo de trabajo está organizado en módulos:

1. Descarga de series desde la API del Banco Central.
2. Transformación mensual y construcción de variaciones interanuales.
3. Integración de calendario de días hábiles.
4. Estimación de modelos para IMACEC total y no minero.
5. Construcción del mes objetivo.
6. Exportación de tablas, gráficos y archivos procesados.

## Métricas de ajuste

```{r metrics-table}
metricas |>
  knitr::kable(
    digits = 2,
    col.names = c("Variable", "N", "RMSE", "MAE", "Nota"),
    caption = "Métricas de ajuste del modelo"
  )
```

::: {.callout-important}
## Lectura prudente

Las métricas anteriores corresponden a ajuste interno del modelo. Sirven para verificar si el ajuste reproduce razonablemente la muestra histórica, pero no deben interpretarse como desempeño predictivo real. Por eso se agrega una evaluación pseudo out-of-sample contra benchmarks simples.
:::

## Evaluación pseudo out-of-sample y benchmarks simples

La evaluación fuera de muestra re-estima el modelo con una **ventana expansiva**: para cada fecha de evaluación, se usa solo información disponible hasta el mes anterior. La predicción se compara contra tres reglas simples:

- **Benchmark AR(1):** modelo autoregresivo de un rezago sobre la variación anual del IMACEC.
- **Promedio móvil 3m:** promedio de las últimas tres variaciones anuales observadas.
- **Naive estacional t-12:** se reutiliza la variación anual observada doce meses antes.

Esta sección es importante porque transforma el proyecto desde un ajuste descriptivo hacia una herramienta evaluable de nowcasting. Un buen modelo aplicado debería mejorar, al menos de forma sistemática, benchmarks parsimoniosos.

```{r oos-table}
oos_display <- oos_metricas |>
  mutate(
    inicio = format(inicio, "%Y-%m"),
    fin = format(fin, "%Y-%m"),
    periodo = paste0(inicio, " a ", fin)
  ) |>
  arrange(variable, rmse) |>
  select(variable, modelo, n, rmse, mae, periodo)

oos_display |>
  knitr::kable(
    digits = 2,
    col.names = c("Variable", "Modelo", "N", "RMSE", "MAE", "Período"),
    caption = "Evaluación pseudo out-of-sample contra benchmarks simples"
  )
```

::: {.callout-note}
## Cómo leer esta tabla

La comparación relevante no es si el modelo tiene error bajo en términos absolutos, sino si reduce RMSE/MAE frente a reglas simples. Si el promedio móvil o el AR(1) dominan al modelo principal, eso es una señal útil: el modelo necesita simplificación, mejores predictores contemporáneos, mejor tratamiento de vintages o una ventana de estimación distinta.

La ventana de evaluación todavía es corta; por lo tanto, estos resultados deben leerse como diagnóstico preliminar de desempeño predictivo, no como validación definitiva del modelo.
:::

## Próximos pasos

- Profundizar la evaluación fuera de muestra con vintages reales de publicación.
- Comparar ventanas expansivas versus ventanas móviles.
- Incorporar intervalos de predicción calibrados con errores históricos fuera de muestra.
- Separar una versión de seguimiento coyuntural y una nota técnica más formal.
- Fortalecer la trazabilidad de vintages y fechas de publicación.
